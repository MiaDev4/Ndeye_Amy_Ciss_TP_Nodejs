<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Ajouter produit</title>
    <style>
      #sty {
        border: 1px solid black;
        border-radius: 5px;
        padding: 10px;
        max-width: 400px;
        margin-bottom: 20px;
      }
      nav a {
        margin-right: 150px;
        text-decoration: none;
        color: blue;
        border: 1px solid black;
        border-radius: 5px;
        padding: 5px 10px;
      }
    </style>
  </head>
  <body>
    <nav>
      <a href="/produits">Accueil</a>
    </nav>

    <h2>Ajouter un produit</h2>

    <% if (erreur) { %>
    <p style="color: red"><%= erreur %></p>
    <% } %>
    <fieldset id="sty">
      <form method="POST" action="/produits">
        Nom :
        <input type="text" name="nom" value="<%= data.nom || '' %>"/><br/><br/>
        Prix (FCFA) :
        <input type="number" name="prix" value="<%= data.prix || '' %>"/><br/><br/>
        Quantité :
        <input type="number" name="quantite" value="<%= data.quantite || '' %>"/><br/><br/>

        Catégorie :
        <select name="categorie">
          <option value="">Catégorie</option>
          <option value="Fourniture">Fourniture</option>
          <option value="Accessoire">Accessoire</option>
          <option value="Électronique">Électronique</option>
        </select><br /><br />

        <button type="submit">Ajouter</button>
      </form>
    </fieldset>
  </body>
</html>
